# Results — Latency (CEXP04 timing)

**Rule:** New Round 2 outputs only.

Expected artifacts:

- `latency_per_stage_*.json`
- Optional bar chart for Efficiency claim in paper

Stages to log: embedding, retrieval, defense (D1+D2+D3), FilterRAG-style B0 (if run), LLM generation, end-to-end.
